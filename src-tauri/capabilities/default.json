{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the DupHunter main window. Local-only: dialog, store, fs asset reads for previews, and opener for revealing files. No network permissions are granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default",
    "dialog:default",
    "store:default",
    "opener:default",
    "fs:default",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "**" }]
    },
    "fs:allow-read-file",
    "fs:read-meta"
  ]
}
